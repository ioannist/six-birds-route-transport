# Search Space Inventory

- `fixed_support_core_small`: `configs/search/fixed_support_core_small.search.json`, raw `256`, capped `64`, smallest conservative fixed-support baseline.
- `cyclic_memory_small`: `configs/search/cyclic_memory_small.search.json`, raw `256`, capped `128`, cyclic/loop-focused space for coherent transported-memory candidates.
- `groupoid_probe_small`: `configs/search/groupoid_probe_small.search.json`, raw `128`, capped `128`, slightly broader fixed-support probe beyond purely cyclic carriers.
