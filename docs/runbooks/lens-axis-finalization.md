# Lens-axis finalization

## Purpose

This runbook closes TH4 by turning two already-established lens-axis outcomes into one canonical axis statement: a narrower same-step bounded-negative subregime and a broader cross-resolution strict-extension flagship that yields an accepted quotient-backed obstruction.

## Original TH4 subregimes

- Same-step bounded-negative:
  - fixed mechanism/configuration
  - fixed packaging family as much as practical
  - same frozen slice, same support, same step
  - accepted proposals remained quotient-feasible
  - candidate-subset quotient witnesses were present

- Cross-resolution strict-extension:
  - fixed mechanism/configuration
  - fixed support object and same run/evaluation regime
  - admissible lens change now spans multiple resolutions on the same support
  - accepted proposals become quotient-infeasible while natural pairing remains feasible

## Why both belong to the lens axis

- The mechanism remains fixed in both subregimes.
- The support object remains fixed in both subregimes.
- What changes is the record algebra induced by admissible lens/projection structure.
- The same-step subregime restricts that variation to one step/resolution.
- The cross-resolution subregime admits strict-extension lens change across multiple resolutions on the same support object.

## What is held fixed

- mechanism/configuration
- packaging family / selector policy as much as practical
- fixed evaluation regime
- observable-first event-package semantics
- current shared-event admissibility
- quotient-backed feasibility audit semantics

## What varies

- Same-step subregime:
  - lens/projection family on one fixed step/resolution slice

- Cross-resolution strict-extension subregime:
  - lens/record algebra across multiple resolutions on the same fixed support

## Canonical lens-axis flagship

- Canonical case ID: `cross_res_all_steps`
- Fixed mechanism: `exp104_p6_row_all_n64`
- Fixed packaging family: `bridge_default_packaging_selector`
- Provenance classification: `admissible`
- Quotient witness status: `accepted_proposal_obstruction`
- Natural-pairing control remains feasible

## Final closure rule

- Preserve the same-step run as a bounded-negative subregime.
- Promote the cross-resolution strict-extension case as the official lens-axis flagship.
- Report the closed lens axis as a finalized two-regime result rather than as two conflicting TH4 outcomes.

## Downstream consumption

- Findings/paper-facing layers should cite `canonical_flagship_case_id` for the main lens-axis witness.
- The same-step negative artifacts remain required controls and should stay linked in the final summary.
- The finalization output, not the earlier provisional TH4 summaries alone, is the authoritative axis-level reference.
